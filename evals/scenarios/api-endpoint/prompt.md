---
fixture: luma-skeleton
---
Expose a REST endpoint GET /V1/acme/brands from the Acme_Catalog module that returns a list of brand names (hard-code three names for now). Store managers should be able to call it; it must not be public. Create every file needed. Do not run bin/magento.
