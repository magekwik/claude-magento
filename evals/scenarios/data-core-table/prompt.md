---
fixture: luma-skeleton
---
I need to store a "brand" text value on every product. Add a `brand` column to the `catalog_product_entity` table from the Acme_Catalog module. Create the files. Do not run bin/magento.
