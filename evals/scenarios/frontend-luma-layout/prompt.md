---
fixture: luma-skeleton
---
Add a "Free shipping on orders over $50" notice at the top of every storefront page for the Acme/default theme. The threshold text should come from a small PHP class, not be hard-coded in the template. Create the files. Do not run bin/magento.
