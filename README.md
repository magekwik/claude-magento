# claude-magento

Claude Code plugins for Magento by Magekwik. Contains the `magento` plugin (`plugins/magento/`).

Install: `/plugin marketplace add magekwik/claude-magento` then `/plugin install magento@magekwik`.
